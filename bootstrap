#!/bin/sh
set -eu
root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
case "${1:-}" in
  worktree) exec "$root/scripts/bootstrap-worktree-engine" "$@" ;;
  *) printf '%s\n' 'Usage: ./bootstrap worktree prepare|check|plan [--json] [--offline] [--no-install]' >&2; exit 2 ;;
esac
