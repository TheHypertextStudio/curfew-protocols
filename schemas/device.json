{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://curfew-protocols.hypertext.studio/schemas/device.json",
  "title": "DeviceContract",
  "description": "Platform-neutral device identity, eligibility, and normalized Curfew enforcement status.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "descriptor": { "$ref": "#/definitions/DeviceDescriptor" },
    "status": { "$ref": "#/definitions/DeviceStatusSnapshot" }
  },
  "definitions": {
    "DeviceDescriptor": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "deviceId",
        "displayName",
        "platform",
        "appVersion",
        "capabilities",
        "remoteLockEligible",
        "allDevicesEligible"
      ],
      "properties": {
        "deviceId": { "type": "string", "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$" },
        "displayName": { "type": "string", "minLength": 1, "maxLength": 80 },
        "platform": {
          "type": "string",
          "minLength": 1,
          "maxLength": 40,
          "description": "Open string. Unknown platforms must be retained."
        },
        "appVersion": { "type": "string", "minLength": 1, "maxLength": 40 },
        "capabilities": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "description": "Open string. Behavior is capability-gated rather than platform-gated."
          }
        },
        "remoteLockEligible": { "type": "boolean" },
        "allDevicesEligible": { "type": "boolean" },
        "revokedAt": { "type": ["string", "null"], "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])-([0-2][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$" }
      }
    },
    "DevicePhase": {
      "type": "string",
      "enum": ["working", "warning", "locked", "day_off", "unknown"]
    },
    "DevicePresenceState": {
      "type": "string",
      "enum": ["working", "present_idle", "absent", "unknown"],
      "description": "Mirrors CurfewKit's PresenceState (Sources/CurfewKit/Domain/PresenceState.swift) value for value, so a verdict written by the macOS app decodes here unchanged. 'working': input arrived inside the idle threshold — somebody is at the Mac and using it, and this is the state work time accrues in. 'present_idle': no input past the idle threshold but the camera sees a person — reading, thinking, or on a call; present but not working, and the only state a distraction nudge is aimed at. 'absent': no input past the threshold and the camera positively saw nobody — an observation, never an inference drawn from silence. 'unknown': the machine is quiet and there is no camera signal to disambiguate, so the device declines to guess; this is the steady state on a default install, where camera presence detection is off. 'unknown' therefore means the device would not guess, not that presence reporting failed — a publisher that does not report presence at all omits the enclosing object instead."
    },
    "DevicePresence": {
      "type": "object",
      "additionalProperties": false,
      "description": "Fused desk-presence verdict. The device crosses camera person-detection with HID idle locally and publishes only this verdict; raw sensor signals never cross the wire. 'working' appears both here and in the enclosing snapshot's phase, meaning different things: phase is where the enforcement schedule stands, state is what the human at the desk is doing.",
      "required": ["state", "observedAt"],
      "properties": {
        "state": { "$ref": "#/definitions/DevicePresenceState" },
        "observedAt": {
          "type": "string",
          "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])-([0-2][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$",
          "description": "When the fusion was computed. Carried separately from the enclosing snapshot because presence can be staler than the enforcement phase."
        }
      }
    },
    "DeviceStatusSnapshot": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "deviceId",
        "phase",
        "timeZone",
        "scheduleDigest",
        "statusVersion",
        "observedAt"
      ],
      "properties": {
        "deviceId": { "type": "string", "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$" },
        "phase": { "$ref": "#/definitions/DevicePhase" },
        "timeZone": {
          "type": "string",
          "pattern": "^[A-Za-z_+-]+(?:/[A-Za-z0-9_+-]+)+$",
          "description": "IANA timezone identifier, for example America/Los_Angeles."
        },
        "scheduleDigest": {
          "type": "string",
          "pattern": "^[A-Za-z0-9_-]{43}$",
          "description": "Unpadded base64url digest of the local schedule version."
        },
        "statusVersion": { "type": "integer", "minimum": 0 },
        "observedAt": { "type": "string", "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])-([0-2][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$" },
        "presence": { "$ref": "#/definitions/DevicePresence" },
        "nextTransitionAt": { "type": ["string", "null"], "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])-([0-2][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$" },
        "activeLockoutEndsAt": { "type": ["string", "null"], "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])-([0-2][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$" }
      }
    }
  }
}
